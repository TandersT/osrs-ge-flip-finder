---
description: Open the GE Flip Finder VS Code workspace file.
allowed-tools:
  - Bash(code:*)
---

Run `code vs-workspace.code-workspace` from the repo root to open the GE Flip Finder multi-root workspace in VS Code. Do nothing else — no status update, no summary, no follow-up questions. If the command errors (e.g. `code` isn't on PATH), report the error verbatim.
